Work only inside the current workspace. Use `.` and workspace-relative paths such as `src/lib.rs` in every tool argument; never pass the absolute workspace path, even if test output displays it. Run the tests, identify the root cause, implement the smallest correct fix, and rerun the focused test suite. Preserve the existing public contract and do not add dependencies. Finish with a short summary of the changed workspace-relative file and the exact verification result.
