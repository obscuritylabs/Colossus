Review the current worktree without modifying files. Use `.` and workspace-relative paths in every tool argument; never pass the absolute workspace path. Inspect the changed files and the surrounding implementation, then report only actionable correctness, security, compatibility, or missing-test findings. Order findings by severity and include a workspace-relative file path plus concrete evidence for each one. If there are no actionable findings, say that explicitly and list the residual testing risks.
