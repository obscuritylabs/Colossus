Use exactly two subagents for a read-only review of the current workspace. Give one subagent the workspace-relative task of reviewing implementation correctness and the other the task of reviewing test coverage. Require each child to use `.` and workspace-relative paths in every tool argument. Do not use absolute paths and do not let either child delegate recursively or modify files. Wait for both results, then synthesize the three highest-priority findings with workspace-relative evidence. If the delegation capability is unavailable, report that limitation instead of simulating child results.
