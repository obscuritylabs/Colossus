Inspect the current workspace using `.` and workspace-relative paths in every tool argument; never pass the absolute workspace path. Then create exactly one durable task for the observed failing behavior, one active decision recording that the fix must preserve the public clamp contract, and one reusable memory describing the focused verification command. List the created records and return their identifiers. Do not modify workspace files or execute the fix.
