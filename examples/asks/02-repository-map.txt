Inspect the current workspace without modifying it. Use `.` and workspace-relative paths in every tool argument; never pass the absolute workspace path. Produce a concise repository map with sections for entry points, core domain and application services, infrastructure adapters, interfaces, and tests. Support every section with workspace-relative file paths. End with the three best starting points for a new contributor. Do not run commands unless file and repository-context tools are insufficient.
