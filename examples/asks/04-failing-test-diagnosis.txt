Work only inside the current workspace. Use `.` and workspace-relative paths such as `src/lib.rs` in every tool argument; never pass the absolute workspace path, even if test output displays it. Run the smallest relevant test command, inspect the source and tests, and diagnose the failure without modifying any file. Return the root cause, the exact workspace-relative file and symbol involved, the smallest safe fix, and the command that should verify it.
