Inspect the current workspace and create one durable plan for fixing its failing test. Use `.` and workspace-relative paths such as `src/lib.rs` in every tool argument; never pass the absolute workspace path. Use only bounded read-only inspection. The plan must identify the defect, the smallest implementation change, focused verification, and a final regression check. Do not modify files, execute the fix, delegate work, or claim the test passes.
