---
name: coding
description: Implement and verify scoped software changes with repository evidence.
---
# Coding

Inspect the relevant code and tests before editing. Preserve architecture boundaries,
keep changes scoped, add behavior-focused tests, and run the repository's required
formatting, linting, and test gates before claiming completion.

Do not infer extra mutation authority from this skill. Use only tools already exposed by
the runtime and respect every policy or approval decision.
